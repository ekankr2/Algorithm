const input = require('fs').readFileSync('input.txt').toString().trim().split('\n')
const N = input[0]
let number = 0
let letterCheck = []

for (let i = 1; i <= N; i++) {
    let word = input[i]

    for (let j = 0; j < word.length; j++) {
        let letter = word[j]

    }
    letterCheck.unshift()
}
